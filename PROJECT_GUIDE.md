# gg-lint 项目使用说明

## 🎉 项目已完成！

所有核心功能已实现，开发服务器正在运行：http://localhost:3000

## 📁 项目结构

```
gg-lint/
├── app/
│   ├── layout.tsx              # 根布局（SEO、PWA 元数据）
│   ├── page.tsx                # 主页面（核心交互逻辑）
│   ├── favicon.ico
│   └── globals.css
├── components/
│   ├── ui/                     # shadcn/ui 组件
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   └── textarea.tsx
│   ├── EditorInput.tsx         # 左侧输入框（支持粘贴富文本）
│   ├── MarkdownPreview.tsx     # 右侧预览（实时渲染）
│   └── CopyButton.tsx          # 一键复制按钮
├── lib/
│   ├── markdown.ts             # 核心转换逻辑
│   └── utils.ts                # shadcn 工具函数
├── public/
│   ├── manifest.json           # PWA 配置
│   └── icons/                  # PWA 图标目录
├── next.config.ts              # Next.js 配置
├── tailwind.config.ts          # Tailwind 配置
├── tsconfig.json
└── package.json
```

## 快速开始

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
npm start
```

### 静态导出（用于部署）

1. 修改 `next.config.ts`，取消注释 `output: "export"`
2. 运行：

```bash
npm run build
```

3. 输出目录：`out/`

## 使用方法

1. **粘贴内容**

   - 在网页上复制内容（Ctrl+C / Cmd+C）
   - 在左侧输入框粘贴（Ctrl+V / Cmd+V）
   - 自动转换为 Markdown
2. **手动转换**

   - 直接输入纯文本
   - 点击「手动转换」按钮
3. **调整标题层级**

   - 点击「提升层级」：# → ## → ###
   - 点击「降低层级」：### → ## → #
4. **复制结果**

   - 点击「复制 Markdown」
   - 粘贴到 Obsidian / Typora / Notion 使用

## 🔧 技术细节

### HTML 转换流程

```
网页内容 (富文本)
    ↓ clipboardData.getData('text/html')
原始 HTML 字符串
    ↓ Turndown
原始 Markdown
    ↓ remark + 自定义插件
修复后的 Markdown
    ↓ 
显示 + 复制
```

### 自定义 remark 插件

1. `remarkFixHeadings` - 修复标题层级
2. `remarkFixEmphasis` - 规范粗体斜体
3. `remarkCleanWhitespace` - 清理空行
4. `remarkFixImages` - 补全图片 alt

## 🎨 样式定制

所有组件使用 shadcn/ui + Tailwind CSS，可轻松定制：

- 修改 `app/globals.css` 调整全局样式
- 修改 `tailwind.config.ts` 调整主题色
- 每个组件都可独立定制

## 📦 部署指南

### Vercel（推荐）

1. 推送到 GitHub
2. 访问 https://vercel.com
3. Import 项目
4. 一键部署

### Cloudflare Pages

1. 推送到 GitHub
2. 访问 Cloudflare Dashboard
3. Pages → Create a project
4. 构建配置：
   - 构建命令：`npm run build`
   - 输出目录：`out`（需启用静态导出）

### Netlify

1. 推送到 GitHub
2. 访问 Netlify
3. Add new site → Import from Git
4. 构建配置自动识别

## 🔮 未来优化建议

### 短期（可选）

- [ ] 添加实际的 PWA 图标（192×192 和 512×512）
- [ ] 添加更多导出格式（HTML、PDF）
- [ ] 支持批量文件转换
- [ ] 添加转换历史记录（LocalStorage）

### 长期（进阶）

- [ ] AI 智能优化（调用 OpenAI API）
- [ ] 支持自定义规则配置
- [ ] 浏览器插件版本
- [ ] 桌面应用（Electron）

## ⚡ 性能优化

已完成的优化：

- ✅ Server Actions（减少客户端 JS）
- ✅ 按需加载组件
- ✅ Turbopack 构建加速
- ✅ 图片优化禁用（静态导出需要）

## 🐛 已知问题

1. **PWA 图标未添加**

   - 需要手动创建 192×192 和 512×512 图标
   - 放置在 `public/` 目录
2. **静态导出与 Server Actions**

   - 当前使用 Server Actions，不能直接静态导出
   - 如需静态导出，需将 Server Actions 改为客户端函数

## 📞 技术支持

遇到问题？

1. 检查 `npm run dev` 是否正常启动
2. 查看浏览器控制台错误
3. 检查 Node.js 版本（需要 18+）

## 🎓 学习资源

- [Next.js 文档](https://nextjs.org/docs)
- [Turndown 文档](https://github.com/mixmark-io/turndown)
- [remark 文档](https://github.com/remarkjs/remark)
- [shadcn/ui 文档](https://ui.shadcn.com/)

---

祝使用愉快！🎉
